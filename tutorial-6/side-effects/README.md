What is side-effect ?
A side effect is anything that reaches outside your component 
Think of a React component like a room

🏠Inside the Room (NOT Side Effects)

These are normal React operations that stay inside the component:

Calculating values
Creating variables
Returning JSX
Managing state with useState

const total = price * quantity;

